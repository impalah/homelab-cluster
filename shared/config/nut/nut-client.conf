# netclient: este nodo solo ejecuta upsmon contra el SAI remoto en pi-obs,
# sin driver ni upsd propios. Mismo motivo que en pi-obs (ver
# pi-obs/config/nut/nut.conf) -- el paquete nut-client de Ubuntu bloquea
# upsmon si MODE=none (valor de fabrica).
MODE=netclient
