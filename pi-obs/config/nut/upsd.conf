# LISTEN en todas las interfaces -- upsd se publica a proposito en la LAN
# (puerto 3493, ver "ports:" en docker-compose.yml) porque hay upsmon
# remotos en los otros 5 nodos y el nut_exporter de Prometheus, mismo
# criterio que postgres-main publicado en 5432 (docs/CLAUDE.md).
LISTEN 0.0.0.0 3493
